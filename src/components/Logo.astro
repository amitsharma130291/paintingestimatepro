---
interface Props {
  class?: string;
}
const { class: className = "" } = Astro.props;
---

<img
  src="/brand/logo-primary-512.png"
  alt="PaintingPricing Calculator"
  width="512"
  height="130"
  decoding="async"
  class={`block h-auto w-[clamp(170px,14vw,215px)] shrink-0 translate-y-[3px] ${className}`}
/>
